{
  "name": "tali",
  "version": "0.0.1",
  "private": true,
  "description": "Unified onchain + offchain money tool for non-trader crypto-active users",
  "packageManager": "pnpm@9.12.0",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "dev:backend": "pnpm --filter @tali/backend dev",
    "dev:web": "pnpm --filter @tali/web dev",
    "dev:cli": "echo 'CLI removed in this branch — use feat/week-1-scaffold for standalone skill'",
    "db:migrate": "pnpm --filter @tali/backend db:migrate",
    "db:generate": "pnpm --filter @tali/backend db:generate",
    "db:reset": "pnpm --filter @tali/backend db:reset",
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "lint": "eslint .",
    "format": "prettier --write \"**/*.{ts,tsx,js,json,md}\"",
    "format:check": "prettier --check \"**/*.{ts,tsx,js,json,md}\""
  },
  "devDependencies": {
    "@types/node": "^22.7.0",
    "eslint": "^9.12.0",
    "prettier": "^3.3.3",
    "typescript": "^5.6.2",
    "typescript-eslint": "^8.8.0"
  }
}
